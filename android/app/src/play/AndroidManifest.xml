<?xml version="1.0" encoding="utf-8"?>
<!--
  Overlay del flavor "play" (Google Play).
  Quita del manifiesto todo lo que Play no permite o ya no hace falta, porque la
  entrega del timbre la hace 100% Firebase Cloud Messaging:
   - REQUEST_INSTALL_PACKAGES: era para el autoactualizador (prohibido en Play).
   - FOREGROUND_SERVICE / _SPECIAL_USE: ya no hay servicio de escucha permanente.
   - REQUEST_IGNORE_BATTERY_OPTIMIZATIONS: uso restringido por política de Play.
   - RECEIVE_BOOT_COMPLETED: solo servía para relanzar el servicio de escucha.
   - RingListenerService / BootReceiver / FileProvider: piezas del modo sideload.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />
    <!-- REQUEST_IGNORE_BATTERY_OPTIMIZATIONS SÍ se mantiene en Play:
         para una app de timbre es una "core function" legítima (entregar el aviso
         al instante aunque el teléfono esté en Doze). En Play Console hay que
         completar la declaración justificando esta política. -->

    <application>
        <service android:name=".service.RingListenerService" tools:node="remove" />
        <receiver android:name=".BootReceiver" tools:node="remove" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            tools:node="remove" />
    </application>
</manifest>
